name: Daily Auto-Commit Trigger

on:
  schedule:
    # Runs at 6 AM UTC every day (11:30 AM IST)
    # Cron format: minute hour day-of-month month day-of-week
    - cron: '0 6 * * *'
  
  # Allow manual trigger from GitHub UI
  workflow_dispatch:

jobs:
  trigger-auto-commit:
    runs-on: ubuntu-latest
    
    steps:
      - name: Trigger Auto-Commit Endpoint
        env:
          APP_URL: ${{ secrets.APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          echo "🚀 Triggering auto-commit at $(date -u)"
          
          response=$(curl -s -w "\n%{http_code}" -X GET "${APP_URL}/api/cron/daily" \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            -H "Content-Type: application/json")
          
          # Extract HTTP status code (last line)
          http_code=$(echo "$response" | tail -n1)
          # Extract response body (all but last line)
          body=$(echo "$response" | sed '$d')
          
          echo "📡 Response Status: $http_code"
          echo "📋 Response Body: $body"
          
          # Check if request was successful
          if [ "$http_code" -ge 200 ] && [ "$http_code" -lt 300 ]; then
            echo "✅ Auto-commit trigger successful!"
            exit 0
          else
            echo "❌ Auto-commit trigger failed with status $http_code"
            exit 1
          fi
      
      - name: Summary
        if: always()
        run: |
          echo "## Daily Auto-Commit Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Triggered at:** $(date -u)" >> $GITHUB_STEP_SUMMARY
          echo "- **Endpoint:** \`/api/cron/daily\`" >> $GITHUB_STEP_SUMMARY
